{
  "name": "peg-solitaire",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel index.html",
    "test": "jest",
    "prebuild": "rm -rf dist",
    "build": "parcel build index.html",
    "postbuild": "rm -rf www && mkdir www && cp dist/** www"
  },
  "license": "MIT",
  "dependencies": {
    "collections": "^5.1.13",
    "jquery": "^3.7.0",
    "ramda": "^0.29.0"
  },
  "devDependencies": {
    "@babel/core": "^7.22.10",
    "@babel/preset-env": "^7.22.10",
    "babel-jest": "^29.6.2",
    "jest": "^29.6.2",
    "parcel": "^2.9.3"
  }
}
